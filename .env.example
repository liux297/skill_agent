INSTALL_METHOD=remote
REMOTE_INSTALL_URL=debug.dify.ai:5003
REMOTE_INSTALL_KEY=********-****-****-****-************

# 命令白名单配置（可选）
# 格式：逗号分隔的命令列表
# 示例：SKILL_AGENT_ALLOWED_COMMANDS=python,pip,grep,curl,git,bash
# 留空或注释掉则使用内置默认白名单（包含 python/pip/node/npm/curl/git/bash 等）
# SKILL_AGENT_ALLOWED_COMMANDS=
